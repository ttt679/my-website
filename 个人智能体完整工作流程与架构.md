---
title: 个人智能体完整工作流程与架构
type: 技术参考
tags:
  - 智能体
  - 微信机器人
  - ReAct
  - 会话管理
  - 架构设计
  - MCP
instruction: "整理为结构化学习笔记，包含完整架构图、启动流程、消息处理流程、会话隔离机制、代码冗余分析和关键设计要点。适合作为个人知识库的技术参考文档。"
---

## 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    用户交互层                             │
├──────────────────┬──────────────────────────────────────┤
│  命令行 CLI      │  微信（双手机方案）                   │
│  python main.py  │  手机B (主账号) → 手机A (AI助手)     │
└────────┬─────────┴──────────────┬───────────────────────┘
         │                        │ Wechaty 监听
         │                        ▼
         │              ┌──────────────────────┐
         │              │ wechat_bridge.js     │
         │              │ (Node.js 进程)       │
         │              │ • 扫码登录/消息过滤  │
         │              │ • HTTP 转发/回复发送 │
         │              └──────────┬───────────┘
         │                         │ HTTP POST
         │                         ▼
         └────────────►┌──────────────────────┐
                       │ FastAPI (端口 8000)  │
                       │ POST /wechat         │
                       │ GET /health          │
                       │ GET /sessions        │
                       └──────────┬───────────┘
                                  │
                                  ▼
                       ┌──────────────────────┐
                       │ SessionManager       │
                       │ • 多会话隔离/线程安全│
                       │ • 追加模式           │
                       └──────────┬───────────┘
                                  │
                                  ▼
                       ┌──────────────────────┐
                       │ run_agent()          │
                       │ (ReAct 循环)         │
                       │ 1.LLM推理 2.工具调用 │
                       │ 3.执行结果 4.生成回复│
                       └──────────┬───────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    ▼             ▼             ▼
          ┌──────────────┐ ┌──────────┐ ┌──────────────┐
          │ MCP Server   │ │ MCP      │ │ Skill        │
          │ (Obsidian)   │ │ Server   │ │ Manager      │
          │              │ │ (FS)     │ │              │
          └──────────────┘ └──────────┘ └──────────────┘
```

## 一、启动流程

```bash
python main.py --wechat
```

### 1.1 环境准备
- 加载 `.env` 中的环境变量（API Key 等）
- 解析命令行参数：`--wechat`、`--refresh-cache`

### 1.2 智能体初始化
调用 `initialize_agent()`，依次完成：
- 加载 MCP Server 配置（`config/mcp_servers.json`）
- 工具发现并缓存至 `cache/mcp_tools_cache.json`
- 注册核心工具白名单（`config/core_tools.json`）
- 加载 Skill 系统

### 1.3 启动 HTTP 服务（后台线程）
- FastAPI 监听 `0.0.0.0:8000`
- 端点：
  - `POST /wechat` → 接收微信消息
  - `GET /health` → 健康检查
  - `GET /sessions` → 列出活跃会话

### 1.4 启动微信桥接子进程
```python
wechat_process = subprocess.Popen(
    ["node", "wechat_bridge.js"],
    env={"API_URL": "http://127.0.0.1:8000/wechat", **os.environ}
)
```
- Node.js 进程通过环境变量获知 API 地址
- 主进程阻塞等待子进程结束

### 1.5 Wechaty 扫码登录
- 控制台输出二维码链接，用户用手机 A（AI 助手账号）扫码
- 登录后即可监听消息

---

## 二、消息处理流程（ReAct 模式）

### 2.1 微信端链路
手机 B 发送消息 → 手机 A 接收 → Wechaty 检测 `message` 事件 → `handleMessage()`  
过滤逻辑：忽略自己发送的消息、仅处理文本消息、提取发送者 ID 和文本内容

### 2.2 HTTP 转发
桥接程序将消息 POST 至 `http://127.0.0.1:8000/wechat`：
```json
{
  "session_id": "wxid_xxx",
  "text": "帮我搜索量子力学"
}
```

### 2.3 FastAPI 处理
```python
response, new_history = run_agent(user_input, history)
session_mgr.append_user_message(session_id, user_input)
session_mgr.append_assistant_message(session_id, response)
```
- 获取当前会话历史
- 调用 `run_agent()` 执行 ReAct 循环
- 将用户消息和助手回复追加到会话历史

### 2.4 ReAct 推理循环
1. **LLM 工具选择**：携带系统提示 + 工具列表，LLM 返回工具调用（如 `obsidian_global_search`）
2. **工具执行**：同步调用对应 MCP 工具，获取结果
3. **追加工具消息**：将工具调用 ID 和结果写入会话历史
4. **二次生成**：LLM 基于工具结果生成最终回复

### 2.5 回复返回
- FastAPI 返回 `{ "reply": "…" }`
- 桥接程序调用 Wechaty API 发送回手机 B

---

## 三、会话隔离机制

### 设计
- **SessionManager** 以 `session_id` 为键管理独立会话列表
- 每个微信用户（基于 `wxid`）拥有独立的历史上下文
- CLI 用户使用固定 `"cli"` 作为 session_id

### 特点
- 完全隔离，互不干扰
- 线程安全：使用 `threading.Lock` 保护内部字典
- 只追加不修改：适合 Prompt 缓存
- 返回深拷贝：防止外部误修改内存中的历史数据

---

## 四、代码冗余与优化建议

| 文件 | 状态 | 建议 |
|------|------|------|
| `PersonalAgentBot.memory-card.json` | 🔴 空文件 | 建议删除 |
| `package-lock.json` | 🟡 锁定文件 | 保留，可加入 `.gitignore` |
| `__pycache__/` | 🟡 字节码 | 已在 `.gitignore` |
| `test_*.py` | 🟢 测试文件 | 保留，辅助开发调试 |
| 文档类文件 | 🟢 可合并 | `CACHE_GUIDE.md`（合并两个缓存文档）<br>`WECHAT_GUIDE.md`（合并两个微信文档） |

---

## 五、关键设计要点

1. **双进程架构**：Python 主进程（智能体 + HTTP） + Node.js 子进程（微信桥接），通过本地 HTTP 解耦
2. **ReAct 推理循环**：LLM 自主决策工具调用与结果整合，支持多步复杂任务
3. **会话隔离**：每用户独立 session，保证上下文互不污染
4. **线程安全**：SessionManager 使用锁机制，保证并发写入安全
5. **工具白名单**：仅向 LLM 暴露核心工具，降低误用风险
6. **渐进式响应**：工具结果逐步追加，LLM 再推理，实现多轮交互