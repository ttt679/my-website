---
title: 智能体架构与产品形式：战略+施工蓝图
type: 架构设计
tags:
  - 智能体
  - 施工蓝图
  - 产品设计
  - 知识管理
  - Obsidian
instruction: |-
  这是一份已整理好的架构设计笔记，需要将新增的"施工蓝图"模块（实体Schema、Skill封装规范、核心工作流）和"开放性问题"补充整合进去，保持原文完整结构的同时插入新内容。请按以下结构组织：

  1. 系统架构演进路径（保留原文）
  2. 核心实体模型设计（保留原文 + 新增2.1实体数据模型规范Schema，含四类实体的YAML字段定义）
  3. 产品形态的范式转变：去SaaS化（保留原文）
  4. Orchestrator与Skill的职责划分（保留原文 + 新增4.1 Skill封装规范，含标准文件结构和manifest.yaml规范）
  5. 三层次项目架构（保留原文）
  6. 关键设计决策记录（保留原文）
  7. 核心工作流详解：一周任务生命周期（新增，含周一至周日的完整工作流描述和工作流全景图）
  8. 开放性问题：数字资产如何保持鲜活（新增，含知识折旧、资产迭代等方向的思考）

  整体定位：从“战略地图”升级为“战略地图+施工蓝图”的完整设计文档。
---

## 1. 系统架构演进路径

本项目的系统架构经历了从原型到可生产化设计的三个演进阶段。

- **第一阶段：简单对话式助手**  
  用户通过微信与一个大模型对话，人工记忆上下文、手动整理产出。问题在于上下文断裂，知识无法沉淀为数字资产。

- **第二阶段：任务编排的初步自动化**  
  引入 Obsidian 作为记忆中枢，通过 MCP 让模型能够读写本地笔记，实现“对话 → 结构化笔记”的闭环。但此时所有逻辑都硬编码在单一智能体内，扩展性差。

- **第三阶段：Orchestrator + Skill 可插拔架构**  
  提出“去 SaaS 化”的本地智能体架构。核心为一个轻量的 Orchestrator，根据用户意图动态加载不同 Skill，每个 Skill 自带提示词、知识库和处理逻辑。所有数据以结构化 YAML 和 Markdown 形式存储在 Obsidian 中，形成可迁移、可延续的数字资产。

## 2. 核心实体模型设计

整个系统的信息骨架由四大核心实体构成，它们分别承载洞察、行动、产出与复用的不同职责。

- **想法/洞察**：从对话或阅读中提炼的片段式认知，是可被验证或扩展的最小知识单元。
- **任务/项目**：带有明确依赖、输入产出和完成状态的行动单元，贯穿日常开发与内容生产。
- **内容资产**：面向外部受众的长文、教程、视频脚本等，从上游洞察与任务成果转化而来。
- **产品组件**：可以被重复调用的提示词、案例库、知识库或模板，是 Skill 的原子构成。

这四类实体通过双向链接和 ID 形成语义网络，支撑智能体进行上下文推理和自动化流转。

### 2.1 实体数据模型规范 (Schema)

为确保智能体和 Obsidian 模板能够无歧义地读写数据，每个实体均定义了严格的 YAML 字段规范。

**想法/洞察**
```yaml
type: insight
id: "20260510-ai-boundary"           # 唯一ID，格式：YYYYMMDD-简短描述
status: draft                        # 可选值：draft, validated, deprecated
title: "人与AI协作的四象限边界"
derived_from: "[[对话-20260509]]"    # 溯源：来自哪次对话或哪篇阅读
quadrant_tag: "human_must"           # 可选，四象限标记
created: 2026-05-10
```

**任务/项目**
```yaml
type: project
id: "20260509-scheduler-engine"      # 唯一ID
status: todo                         # 可选值：todo, in-progress, blocked, done, abandoned
title: "开发日程依赖引擎 v1"
deadline: 2026-05-15                 # 日期格式：YYYY-MM-DD
estimated_hours: 6
dependencies:                        # 依赖的其他任务 ID 列表
  - "20260505-learn-mcp"
input_assets:                        # 所需上游资产，对应内容资产的标题
  - "MCP鉴权流程草图"
output: "可运行的依赖图计算模块"      # 本任务完成后产出的资产描述
content_link: "[[项目-日程依赖引擎]]" # 产出资产的 Obsidian 链接
quadrant: ai_easy                    # 可选值：human_must, human_easy, ai_easy, ai_must
post_mortem: "[[复盘-日程依赖引擎]]" # 必须完成的复盘笔记链接
```

**内容资产**
```yaml
type: content
id: "20260518-tutorial-auth"
status: draft                        # 可选值：outline, draft, review, published
title: "本地智能体安全：MCP鉴权实战"
channel: "公众号,掘金"               # 发布渠道
promotes: "[[任务-搭建日程框架]]"     # 关联的推广任务
based_on:                            # 引用的想法或上游笔记
  - "[[踩坑笔记-MCP鉴权]]"
publish_url: ""                      # 发布后的链接（发布后回填）
published_date: ""
```

**产品组件**
```yaml
type: product-component
id: "prompt-quadrant-v1"
component_type: prompt               # 可选值：prompt, case-study, knowledge-base, template
version: "1.0"
title: "四象限分析器提示词 v1"
derived_from: "[[想法-四象限边界]]"   # 溯源到原始想法
status: internal                     # 可选值：internal, beta, released, deprecated
description: "基于人机协作四象限框架的评估提示词，用于引导用户进行任务分工分析"
```

这份 Schema 是后续与通义灵码协作时最核心的“接口定义”，有了它，数据结构和校验逻辑就不会产生歧义。

## 3. 产品形态的范式转变：去 SaaS 化

传统 AI 生产力工具依赖云端 SaaS，存在数据所有权模糊、服务中断风险和长期成本不可控等问题。本项目的核心理念是**将智能体运行在用户本地**，以 Obsidian 为数据中枢，以本地文件系统为持久层。

- **数据主权归属用户**：所有想法、任务、内容和组件均以纯文本 Markdown/YAML 存储，可被任意文本工具读取、版本控制（Git）和迁移。
- **离线可用，按需联网**：核心逻辑在本地执行，仅在需要大模型推理时才调用 API，且通过 MCP 隔离具体实现。
- **技能即文件**：Skill 表现为一个文件夹，包含 prompt、案例和 manifest，安装一个技能就是拷贝一个文件夹，不再依赖商业市场。

这种“本地优先”的架构使得整个系统成为用户长期可维护、可进化的数字外脑，而非短期服役的 SaaS 租户。

## 4. Orchestrator 与 Skill 的职责划分

- **Orchestrator（调度中心）**  
  负责解析用户意图、匹配并加载 Skill、管理上下文状态、调度 MCP 工具调用。它自身不包含任何领域知识，仅作为一个“线程管理器”，确保不同 Skill 之间正确传递信息和依赖。

- **Skill（可插拔能力包）**  
  每一个 Skill 是一个独立的领域专家，内置专属提示词、案例库、知识框架和明确的触发条件。Skill 可被独立开发、版本化和分发，由用户按需安装。

这种分离让系统在功能和知识上具备了“乐高式”的扩展能力，同时保持核心调度逻辑的稳定。

### 4.1 Skill 封装规范 (Skill Manifest)

为实现 Skill 的标准化封装与自动发现，我们定义了一套文件结构和声明格式。

**标准文件结构**
```
skills/
└── quadrant-evaluator/          # 技能包名（kebab-case）
    ├── prompt.md                # 该技能专用的系统提示词
    ├── knowledge/               # 领域知识库文件夹
    │   ├── cases.md             #   案例库
    │   └── framework.md         #   方法论详解
    ├── manifest.yaml            # 技能声明文件（智能体识别的“身份证”）
    └── README.md                # 人类阅读的说明文档
```

**manifest.yaml 规范**
```yaml
name: "quadrant-evaluator"
version: "1.0"
description: "基于四象限框架，帮你分析具体任务中人与AI的最优分工。"
author: "你的名字"
capabilities:                    # 该技能能处理的能力标签列表
  - "analyze_task"
  - "suggest_automation"
triggers:                        # 触发该技能的典型用户意图描述
  - "人和AI怎么分工"
  - "哪些任务交给AI做"
  - "评估团队AI协作"
requires_access:                 # 运行时需要访问的用户数据
  - user_project_notes           #   用户的当前项目资料
  - user_work_habits             #   用户的工作习惯记录
returns: "分工方案 + 四象限可视化 + 落地建议"
prompt_file: "prompt.md"
knowledge_dir: "knowledge/"
```

**设计原则**

- **自主权**：技能包不依赖云端服务，所有逻辑在用户本地智能体上运行
- **声明式**：manifest.yaml 清晰告知 Orchestrator“我能做什么、需要什么、产出什么”
- **版本化**：每个技能包拥有独立版本号，支持迭代升级和向后兼容

这份规范是将“安装一个专家技能”从概念变为可执行技术方案的关键“施工蓝图”。

## 5. 三层次项目架构

系统在设计层面被组织为三个清晰分离的层次：

- **基础设施层**  
  以 Obsidian vault 为核心，结合 Git 版本控制、本地文件系统和 MCP 协议，提供统一的读写接口与数据持久化。

- **智能体调度层**  
  即 Orchestrator，负责意图解析、Skill 生命周期管理、多轮对话上下文维系以及工具链编排。

- **技能与内容层**  
  由一系列 Skill 包和其产生的结构化内容资产（笔记、案例、模板）构成。这一层是真正面向领域问题、可不断生长和迭代的部分。

三层次分离保障了系统的可维护性：更换底层存储只需适配 MCP 接口，扩展新能力只需开发一个新 Skill，核心调度逻辑可以长期稳定。

## 6. 关键设计决策记录

以下是保证架构完整性的核心设计选择，所有后续实施都应以此为约束：

- **以 YAML Front matter 作为结构化数据的唯一载体**：比自定义 DSL 更易解析，又比纯自然语言更可靠。
- **所有实体通过唯一 ID 建立引用关系**：ID 格式 `YYYYMMDD-short-slug`，兼顾可读性和唯一性。
- **任务与内容资产通过 `input_assets` 和 `dependencies` 显式声明依赖**：使得阻塞解除和自动下游触发成为可能。
- **采用 Markdown 双向链接 `[[...]]` 作为人类可读的引用语法**：既能在 Obsidian 中可视化浏览，又能被 MCP 程序化提取。
- **Skill 的封装必须包含 `manifest.yaml`，且声明 triggers、capabilities 和 returns**：以保证 Orchestrator 能够自动发现和匹配技能。
- **所有关键流转（任务完成、发布准备）均通过规则引擎自动校验，而非依赖人类记忆**：从设计上杜绝遗漏和遗忘。

## 7. 核心工作流详解：一周任务生命周期

以下将前述所有概念串联为一个从周一到周日的动态生产线。

**周一 · 意图设定**
- 动作：在微信中与智能体对话，共同确定本周核心主题
- 系统行为：Orchestrator 调用“任务管理” Skill，在 Obsidian 指定项目文件中创建或更新本周核心目标，同步更新所有关联任务状态
- 产出：一份包含本周关键任务、依赖关系和预期产出的周计划笔记

**周二至周五 · 开发与内容生产循环**

上午——开发推进：
- 通过 iLink 发送快捷指令 `/todo`，获取今日优先级最高的待办任务
- 完成开发任务后发送 `/done 任务ID`
- 系统调用 `complete_task_check`，通过 Obsidian MCP 校验产出笔记是否合规（存在、字数达标），然后自动解锁依赖它的下游内容创作任务
- 微信推送阻塞解除通知：“'后端API开发'已完成，以下任务已解除阻塞：撰写教程文章、录制视频脚本”

下午——内容创作：
- 切换到内容创作模式，智能体根据当前可用的上游素材（input_assets）辅助写作
- Orchestrator 调用“写作辅助” Skill，读取对应 Obsidian 笔记，生成初稿或润色建议

**周六 · 复盘与发布冲刺**
- Orchestrator 调用“四象限评估” Skill，基于本周所有已标记 quadrant 字段的任务，生成周度人机协作报告
- 同时扫描所有 publish_platform 字段不为空且状态为 draft/review 的内容资产，生成发布清单并推送至微信
- 产出：发布清单 + 四象限复盘报告

**周日 · 探索与迭代**
- 回顾上一周的所有产出和想法，与智能体进行开放式头脑风暴
- Orchestrator 调用“洞察提炼” Skill，扫描本周新建的 type: insight 笔记，讨论哪些值得进入下周的开发/内容管道
- 产出：新的想法种子，作为下周循环的起点

**工作流全景图**

周一(意图) → 周二~五(开发⇄内容循环) → 周六(复盘+发布) → 周日(探索)
    ↑                                                          │
    └──────────────── 新洞察反馈至下周 ──────────────────────────┘

## 8. 开放性问题：数字资产如何保持“鲜活”？

这是一个暂不要求立即回答，但将在未来决定系统能走多远的设计问题。

随着时间推移，Obsidian 里会自然堆积：过时的想法（三周前的灵感已不再相关）、废弃的版本（提示词 v1.0 已被 v2.0 替代）、失效的发布链接（文章已迁移，原链接 404）。

系统是仅仅记录和依赖这些资产，还是需要一个主动的“资产健康管理”机制？

可能的方向：
- **知识折旧提醒**：智能体定期提示——“这篇文章的核心观点已发布 3 个月，是否需要根据新认知迭代 v2？”
- **产品组件反馈回路**：当某个产品组件的提示词被调用 N 次后，自动收集用户反馈中的高频问题，提醒启动一次优化迭代
- **僵尸资产清理**：每季度扫描一次，标记超过 X 个月未被任何任务或内容引用的孤立笔记，询问归档还是删除
- **影响力追踪**：已发布的内容资产，定期抓取阅读量/互动数据，回写到笔记的 front matter 中，形成“内容资产价值衰减曲线”

这种对知识折旧和资产迭代的主动管理，会让你从“整理得很好的人”变成“让系统持续增值的人”，也是未来产品可以提供的更高阶价值——不只帮用户搭建系统，更帮用户的系统保持生命力。