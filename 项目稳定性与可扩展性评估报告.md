
## 一、整体架构概览

项目采用**四层解耦架构**（接口层→调度层→业务层→数据层），通过 `protocols.py` 中三个 dataclass 实现层间通信。当前有 3 个 Skill、6 个核心工具、2 个 MCP Server、2 个接口渠道（CLI + 微信）。架构方向正确，但落地执行中存在若干关键问题。

---

## 二、稳定性分析

### 🔴 严重问题（2 项）

**1. MCP 连接模式存在根本性缺陷**

当前 `tools/` 下所有客户端函数都采用「每次调用新建连接」的模式：

```
调用工具 → asyncio.run() 创建新事件循环 → 启动 MCP Server 子进程 → MCP 握手 → 执行 → 断开
```

这意味着一次对话中的 5 个工具调用 = 5 次 Node.js 子进程启动 + 5 次 MCP 握手。实际表现是：
- 每个工具调用延迟 3-8 秒（主要是 Node.js 冷启动）
- `asyncio.run()` 在已有事件循环的环境中会报错
- 子进程泄漏风险（异常时连接未正确关闭）
- 在 `general_assistant` 的 ReAct 循环中，这会逐轮叠加延迟

**2. 会话历史未传递给 Skill，多轮对话上下文断裂**

这是最隐蔽但影响最大的问题。看代码链路：

```
main.py cli_loop():
    session_mgr.append_user_message(...)     # ← 往 SessionManager 写
    session_mgr.append_assistant_message(...)
    response = dispatch(user_msg)            # ← 但 dispatch 不读取 SessionManager

orchestrator.dispatch():
    skill_result = _execute_skill(skill_path, msg)   # ← 只传当前消息

general_assistant/main.py execute(msg):
    messages = [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": msg.message}     # ← 只有当前消息！
    ]
```

**结果**：`SessionManager` 只被写入但从未被读取。每一轮对话对 LLM 来说都是"全新会话"，没有上下文记忆。这直接废掉了多轮对话能力。

### 🟡 中等问题（4 项）

**3. 双重工具系统并存**

- `agent_initializer.py` 把核心工具注册到 `tool_registry`（带 schema）
- `mcp_discovery.py` 把 MCP Server 发现的工具存入 `ALL_MCP_TOOLS` 全局变量（仅用于 `/search_tools`）

两个列表不同步。核心工具白名单 (`core_tools.json`) 与 MCP 实际工具之间没有校验——如果 MCP Server 改名/删除了某个工具，白名单不会感知。

**4. JSON/HTML 提取逻辑脆弱**

`storyboard_generator.py` 和 `html_presentation_generator.py` 中用字符串分割提取 LLM 输出：

```python
if "```json" in json_str:
    json_str = json_str.split("```json")[1].split("```")[0].strip()
```

LLM 输出的格式稍有变化（比如多一个空格、用了 ````JSON` 大写）就会解析失败。

**5. 错误处理模式不一致**

- 有的函数返回错误字符串（`"错误：xxx"`）
- 有的抛出异常
- 有的返回带 `"status": "error"` 的字典

调用方需要通过字符串前缀匹配来判断是否出错，非常脆弱。

**6. `asyncio.run()` 在同步包装器中的隐患**

```python
def search_obsidian_notes_sync(query):
    return asyncio.run(search_obsidian_notes(query))
```

`asyncio.run()` 不能在已有事件循环的线程中调用。如果将来有代码在 async 上下文中调用这些同步包装器，会直接抛 `RuntimeError`。

### 🟢 轻微问题（2 项）

**7. `main.py` 中 `cli_loop()` 存在重复派发逻辑**

`get_user_input()` 中对 `@` 命令做了特殊处理直接调用 orchestrator，但 `cli_loop()` 中又对所有消息统一调用 `dispatch()`。这意味着 `@` 命令可能被处理两次。

**8. `skills/general_assistant/main.py` 中的 SYSTEM_PROMPT 是硬编码字符串**

160+ 行的提示词直接写在代码中，修改需要改代码、无法版本管理、无法 A/B 测试。

---

## 三、可扩展性分析

### ✅ 做得好的

| 维度 | 评价 |
|------|------|
| Skill 注册机制 | manifest.yaml + 动态导入，新增 Skill 无需改核心代码 |
| 通信协议 | 三个标准 dataclass，接口层/调度层/业务层解耦清晰 |
| 多渠道支持 | CLI 和微信共用同一套 dispatch 逻辑 |
| 工具注册表 | `tool_registry` 提供了统一的工具调用入口 |

### ❌ 存在障碍的

| 维度 | 问题 | 严重度 |
|------|------|--------|
| 意图识别 | 仅支持 `@命令` 匹配，无法做语义理解。新增 Skill 必须绑定硬编码触发词 | 中 |
| 配置管理 | 配置分散在 6+ 个位置（.env / core_tools.json / mcp_servers.json / tasks/*.json / manifest.yaml / config.json），无统一加载机制 | 中 |
| Skill 生命周期 | 无启用/禁用/热重载机制，修改 manifest 需要重启 | 低 |
| 跨层调用约束 | 没有运行时强制校验，靠约定。`note_organizer/main.py` 直接 import `tools.dialogue_utils` 绕过了 `tool_registry` | 中 |
| 中间件/钩子 | 无。无法在请求链路中插入日志、审计、限流等横切关注点 | 低 |
| 测试 | 项目中没有测试文件。`CLEANUP_COMPLETE.md` 提到删除了 7 个冗余测试文件，但没有替换 | 高 |

---

## 四、使用流程总结

### 当前实际可用的流程

```
┌─────────────────────────────────────────────────────┐
│                    启动入口                          │
│  python main.py                   (纯 CLI)          │
│  python main.py --wechat          (CLI + HTTP API)  │
│  python main.py --refresh-cache   (强制刷新工具缓存) │
└──────────────────────┬──────────────────────────────┘
                       │
         ┌─────────────┴─────────────┐
         ▼                           ▼
    ┌──────────┐              ┌──────────────┐
    │ CLI 渠道 │              │  微信渠道     │
    │ input()  │              │ HTTP /wechat  │
    └────┬─────┘              └──────┬───────┘
         │                           │
         └───────────┬───────────────┘
                     ▼
            ┌─────────────────┐
            │  Orchestrator   │
            │  意图分析(@匹配) │
            └────────┬────────┘
                     │
       ┌─────────────┼─────────────┐
       ▼             ▼             ▼
  ┌─────────┐ ┌──────────┐ ┌──────────────┐
  │ @笔记   │ │ @视频    │ │ 默认(无@)     │
  │ 整理对话│ │ 生成脚本 │ │ ReAct 对话    │
  └────┬────┘ └────┬─────┘ └──────┬───────┘
       │           │              │
       ▼           ▼              ▼
  保存到      生成分镜脚本    工具调用循环
  Obsidian    + HTML演示      (最多10轮)
```

### 实际能做的事情

| 能力 | 状态 | 说明 |
|------|------|------|
| 基础对话 | ⚠️ 单轮可用 | 多轮上下文已断裂 |
| 搜索 Obsidian 笔记 | ✅ 可用 | 每次新建 MCP 连接，较慢 |
| 读取 Obsidian 笔记 | ✅ 可用 | 同上 |
| 保存整理后的笔记 | ✅ 可用 | 通过 `@笔记` 或直接调用 `refine_dialogue` |
| 查看项目结构 | ✅ 可用 | 每次新建 filesystem MCP 连接 |
| 读写项目文件 | ✅ 可用 | 同上 |
| 生成视频分镜+HTML | ⚠️ 部分可用 | JSON/HTML 解析可能因 LLM 输出格式变化而失败 |
| 微信交互 | ✅ 可用 | 需额外启动 `wechat_gateway.py` |
| 多轮上下文对话 | ❌ 不可用 | SessionManager 未接入 Skill |

---

## 五、扩展能力如何切实落地

### 场景 A：新增一个 Skill（如「每日简报生成器」）

**当前需要的步骤**：
1. 创建 `skills/daily_brief/manifest.yaml`，配置 triggers
2. 创建 `skills/daily_brief/main.py`，实现 `execute(msg) -> SkillResult`
3. 创建 `tasks/简报.json` 配置模板
4. 重启应用

**当前可行度**：基本可行，但缺少模板脚手架和文档引导。如果 Skill 需要调用工具，必须了解 `tool_registry` 的 API。

### 场景 B：新增一个消息渠道（如 Telegram）

**当前需要的步骤**：
1. 创建 `interfaces/telegram.py`，实现 `receive_message()` 和 `send_response()`
2. 在 `main.py` 中添加 Telegram bot 的事件循环
3. 复用 `dispatch()` 即可

**当前可行度**：较好。接口层的设计确实做到了渠道无关。

### 场景 C：新增一个 MCP Server（如数据库查询工具）

**当前需要的步骤**：
1. 在 `config/mcp_servers.json` 添加 server 配置
2. 在 `agent_initializer.py` 中注册新工具到 `tool_registry`
3. 在 `config/core_tools.json` 决定是否列入白名单
4. 重启应用

**当前可行度**：可以工作，但新增的 MCP 工具同样面临「每次调用新建连接」的问题。

### 要让扩展真正落地，需要修复的关键点

| 优先级 | 修复项 | 影响 |
|--------|--------|------|
| P0 | MCP 连接池/长连接复用 | 整体响应速度提升 3-5 倍 |
| P0 | SessionManager 接入 Skill | 恢复多轮对话能力 |
| P1 | 统一的配置加载模块 | 新增 Skill/渠道 只需改一处 |
| P1 | 提取 JSON/HTML 使用结构化输出 | 消除解析脆弱性 |
| P2 | 意图识别引入 LLM | 支持语义匹配，不再依赖硬编码触发词 |
| P2 | Skill 脚手架生成器 | `python main.py --create-skill my_skill` |
| P2 | 测试框架搭建 | 保证扩展不破坏现有功能 |

---

## 六、一句话总结

> **架构设计方向正确，四层解耦和协议标准化是好的基础。但两个致命问题（MCP 连接每次重建、会话历史未传入 Skill）让当前系统在稳定性和对话体验上打了大折扣。修复 P0 两项后，扩展能力才能真正落地。**

智能体: 数据已经收集完毕，以下是我的完整评估汇报：

---

# 项目稳定性与可扩展性评估报告

## 一、整体架构概览

项目采用**四层解耦架构**（接口层→调度层→业务层→数据层），通过 `protocols.py` 中三个 dataclass 实现层间通信。当前有 3 个 Skill、6 个核心工具、2 个 MCP Server、2 个接口渠道（CLI + 微信）。架构方向正确，但落地执行中存在若干关键问题。

---

## 二、稳定性分析

### 🔴 严重问题（2 项）

**1. MCP 连接模式存在根本性缺陷**

当前 `tools/` 下所有客户端函数都采用「每次调用新建连接」的模式：

```
调用工具 → asyncio.run() 创建新事件循环 → 启动 MCP Server 子进程 → MCP 握手 → 执行 → 断开
```

这意味着一次对话中的 5 个工具调用 = 5 次 Node.js 子进程启动 + 5 次 MCP 握手。实际表现是：
- 每个工具调用延迟 3-8 秒（主要是 Node.js 冷启动）
- `asyncio.run()` 在已有事件循环的环境中会报错
- 子进程泄漏风险（异常时连接未正确关闭）
- 在 `general_assistant` 的 ReAct 循环中，这会逐轮叠加延迟

**2. 会话历史未传递给 Skill，多轮对话上下文断裂**

这是最隐蔽但影响最大的问题。看代码链路：

```
main.py cli_loop():
    session_mgr.append_user_message(...)     # ← 往 SessionManager 写
    session_mgr.append_assistant_message(...)
    response = dispatch(user_msg)            # ← 但 dispatch 不读取 SessionManager

orchestrator.dispatch():
    skill_result = _execute_skill(skill_path, msg)   # ← 只传当前消息

general_assistant/main.py execute(msg):
    messages = [
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": msg.message}     # ← 只有当前消息！
    ]
```

**结果**：`SessionManager` 只被写入但从未被读取。每一轮对话对 LLM 来说都是"全新会话"，没有上下文记忆。这直接废掉了多轮对话能力。

### 🟡 中等问题（4 项）

**3. 双重工具系统并存**

- `agent_initializer.py` 把核心工具注册到 `tool_registry`（带 schema）
- `mcp_discovery.py` 把 MCP Server 发现的工具存入 `ALL_MCP_TOOLS` 全局变量（仅用于 `/search_tools`）

两个列表不同步。核心工具白名单 (`core_tools.json`) 与 MCP 实际工具之间没有校验——如果 MCP Server 改名/删除了某个工具，白名单不会感知。

**4. JSON/HTML 提取逻辑脆弱**

`storyboard_generator.py` 和 `html_presentation_generator.py` 中用字符串分割提取 LLM 输出：

```python
if "```json" in json_str:
    json_str = json_str.split("```json")[1].split("```")[0].strip()
```

LLM 输出的格式稍有变化（比如多一个空格、用了 ````JSON` 大写）就会解析失败。

**5. 错误处理模式不一致**

- 有的函数返回错误字符串（`"错误：xxx"`）
- 有的抛出异常
- 有的返回带 `"status": "error"` 的字典

调用方需要通过字符串前缀匹配来判断是否出错，非常脆弱。

**6. `asyncio.run()` 在同步包装器中的隐患**

```python
def search_obsidian_notes_sync(query):
    return asyncio.run(search_obsidian_notes(query))
```

`asyncio.run()` 不能在已有事件循环的线程中调用。如果将来有代码在 async 上下文中调用这些同步包装器，会直接抛 `RuntimeError`。

### 🟢 轻微问题（2 项）

**7. `main.py` 中 `cli_loop()` 存在重复派发逻辑**

`get_user_input()` 中对 `@` 命令做了特殊处理直接调用 orchestrator，但 `cli_loop()` 中又对所有消息统一调用 `dispatch()`。这意味着 `@` 命令可能被处理两次。

**8. `skills/general_assistant/main.py` 中的 SYSTEM_PROMPT 是硬编码字符串**

160+ 行的提示词直接写在代码中，修改需要改代码、无法版本管理、无法 A/B 测试。

---

## 三、可扩展性分析

### ✅ 做得好的

| 维度 | 评价 |
|------|------|
| Skill 注册机制 | manifest.yaml + 动态导入，新增 Skill 无需改核心代码 |
| 通信协议 | 三个标准 dataclass，接口层/调度层/业务层解耦清晰 |
| 多渠道支持 | CLI 和微信共用同一套 dispatch 逻辑 |
| 工具注册表 | `tool_registry` 提供了统一的工具调用入口 |

### ❌ 存在障碍的

| 维度 | 问题 | 严重度 |
|------|------|--------|
| 意图识别 | 仅支持 `@命令` 匹配，无法做语义理解。新增 Skill 必须绑定硬编码触发词 | 中 |
| 配置管理 | 配置分散在 6+ 个位置（.env / core_tools.json / mcp_servers.json / tasks/*.json / manifest.yaml / config.json），无统一加载机制 | 中 |
| Skill 生命周期 | 无启用/禁用/热重载机制，修改 manifest 需要重启 | 低 |
| 跨层调用约束 | 没有运行时强制校验，靠约定。`note_organizer/main.py` 直接 import `tools.dialogue_utils` 绕过了 `tool_registry` | 中 |
| 中间件/钩子 | 无。无法在请求链路中插入日志、审计、限流等横切关注点 | 低 |
| 测试 | 项目中没有测试文件。`CLEANUP_COMPLETE.md` 提到删除了 7 个冗余测试文件，但没有替换 | 高 |

---

## 四、使用流程总结

### 当前实际可用的流程

```
┌─────────────────────────────────────────────────────┐
│                    启动入口                          │
│  python main.py                   (纯 CLI)          │
│  python main.py --wechat          (CLI + HTTP API)  │
│  python main.py --refresh-cache   (强制刷新工具缓存) │
└──────────────────────┬──────────────────────────────┘
                       │
         ┌─────────────┴─────────────┐
         ▼                           ▼
    ┌──────────┐              ┌──────────────┐
    │ CLI 渠道 │              │  微信渠道     │
    │ input()  │              │ HTTP /wechat  │
    └────┬─────┘              └──────┬───────┘
         │                           │
         └───────────┬───────────────┘
                     ▼
            ┌─────────────────┐
            │  Orchestrator   │
            │  意图分析(@匹配) │
            └────────┬────────┘
                     │
       ┌─────────────┼─────────────┐
       ▼             ▼             ▼
  ┌─────────┐ ┌──────────┐ ┌──────────────┐
  │ @笔记   │ │ @视频    │ │ 默认(无@)     │
  │ 整理对话│ │ 生成脚本 │ │ ReAct 对话    │
  └────┬────┘ └────┬─────┘ └──────┬───────┘
       │           │              │
       ▼           ▼              ▼
  保存到      生成分镜脚本    工具调用循环
  Obsidian    + HTML演示      (最多10轮)
```

### 实际能做的事情

| 能力 | 状态 | 说明 |
|------|------|------|
| 基础对话 | ⚠️ 单轮可用 | 多轮上下文已断裂 |
| 搜索 Obsidian 笔记 | ✅ 可用 | 每次新建 MCP 连接，较慢 |
| 读取 Obsidian 笔记 | ✅ 可用 | 同上 |
| 保存整理后的笔记 | ✅ 可用 | 通过 `@笔记` 或直接调用 `refine_dialogue` |
| 查看项目结构 | ✅ 可用 | 每次新建 filesystem MCP 连接 |
| 读写项目文件 | ✅ 可用 | 同上 |
| 生成视频分镜+HTML | ⚠️ 部分可用 | JSON/HTML 解析可能因 LLM 输出格式变化而失败 |
| 微信交互 | ✅ 可用 | 需额外启动 `wechat_gateway.py` |
| 多轮上下文对话 | ❌ 不可用 | SessionManager 未接入 Skill |

---

## 五、扩展能力如何切实落地

### 场景 A：新增一个 Skill（如「每日简报生成器」）

**当前需要的步骤**：
1. 创建 `skills/daily_brief/manifest.yaml`，配置 triggers
2. 创建 `skills/daily_brief/main.py`，实现 `execute(msg) -> SkillResult`
3. 创建 `tasks/简报.json` 配置模板
4. 重启应用

**当前可行度**：基本可行，但缺少模板脚手架和文档引导。如果 Skill 需要调用工具，必须了解 `tool_registry` 的 API。

### 场景 B：新增一个消息渠道（如 Telegram）

**当前需要的步骤**：
1. 创建 `interfaces/telegram.py`，实现 `receive_message()` 和 `send_response()`
2. 在 `main.py` 中添加 Telegram bot 的事件循环
3. 复用 `dispatch()` 即可

**当前可行度**：较好。接口层的设计确实做到了渠道无关。

### 场景 C：新增一个 MCP Server（如数据库查询工具）

**当前需要的步骤**：
1. 在 `config/mcp_servers.json` 添加 server 配置
2. 在 `agent_initializer.py` 中注册新工具到 `tool_registry`
3. 在 `config/core_tools.json` 决定是否列入白名单
4. 重启应用

**当前可行度**：可以工作，但新增的 MCP 工具同样面临「每次调用新建连接」的问题。

### 要让扩展真正落地，需要修复的关键点

| 优先级 | 修复项 | 影响 |
|--------|--------|------|
| P0 | MCP 连接池/长连接复用 | 整体响应速度提升 3-5 倍 |
| P0 | SessionManager 接入 Skill | 恢复多轮对话能力 |
| P1 | 统一的配置加载模块 | 新增 Skill/渠道 只需改一处 |
| P1 | 提取 JSON/HTML 使用结构化输出 | 消除解析脆弱性 |
| P2 | 意图识别引入 LLM | 支持语义匹配，不再依赖硬编码触发词 |
| P2 | Skill 脚手架生成器 | `python main.py --create-skill my_skill` |
| P2 | 测试框架搭建 | 保证扩展不破坏现有功能 |

---

## 六、一句话总结

> **架构设计方向正确，四层解耦和协议标准化是好的基础。但两个致命问题（MCP 连接每次重建、会话历史未传入 Skill）让当前系统在稳定性和对话体验上打了大折扣。修复 P0 两项后，扩展能力才能真正落地。**